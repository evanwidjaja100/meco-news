services:
  meco-news:
    build:
      context: .
      args:
        PYTHON_IMAGE: ${PYTHON_IMAGE:-docker.io/library/python:3.14-slim-bookworm}
    image: meco-news:local
    restart: unless-stopped
    stop_grace_period: 30s
    init: true
    read_only: true
    user: "10001:10001"
    env_file:
      - path: .env
        required: false
    environment:
      STATE_DB: /app/data/meco_news.db
      MECO_CONFIG: /app/config/watchlist.json
      UMASK: "077"
    volumes:
      - meco-news-data:/app/data
      - ./config:/app/config:ro
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 128
    mem_limit: 512m
    cpus: 1.0
    command: ["--daemon", "--run-now"]

volumes:
  meco-news-data:
